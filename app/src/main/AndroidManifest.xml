<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      No special permissions are needed by the app that *exposes* AppFunctions (added in
      Phase 2): support is auto-detected and the EXECUTE_APP_FUNCTIONS permission belongs to the
      caller/agent, not to us. Phase 1 is a plain offline app, so it declares no permissions.
    -->

    <application
        android:name=".AtmApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AgenticTaskManager">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AgenticTaskManager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
